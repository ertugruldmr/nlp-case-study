"""ftlink-app: switchable-scenario demo layer over the ftlink pipeline.

The pipeline under test lives in ../deliverable and is consumed strictly as a
library; nothing in this package writes into the deliverable tree.
"""
